<% title 'My Articles' %>
<% page_description 'My Articles' %>

<% @articles = current_user.team_z_profile.articles.order('created_at DESC').page(params[:page]).per(15) %>

<h1>My Articles</h1>

<div id='content'>
  <%= link_to 'Create New Article', new_team_z_article_path, :class => 'button right spacer mr mb-m' %>
  <br/>
  <%= render :partial => 'index_table', :locals => { :articles => @articles } %>  
</div>