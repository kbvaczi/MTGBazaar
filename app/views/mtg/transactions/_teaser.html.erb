<tr class=<%= cycle("odd","even") %>> 
	<td><%= transaction.created_at.strftime("%m/%d/%y") %></td>
  <td><%= link_to transaction.transaction_number, show_mtg_transaction_path(transaction) %></td>
 	<td>
 	  <% if current_user.id == transaction.buyer_id %>
       <%= link_to transaction.seller.username, user_path(transaction.seller) rescue "ERROR" %>
    <% else %>
       <%= link_to transaction.buyer.username, user_path(transaction.buyer) rescue "ERROR" %>
    <% end %>
  </td>
	<td><%= transaction.status %></td>      
  <td><%= transaction.items.to_a.sum(&:quantity_available) %></td>
  <td><%= number_to_currency(transaction.total_value) %></td>      
</tr>
        
      

