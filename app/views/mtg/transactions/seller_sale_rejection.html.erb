<% title "Reject Sale"%>

<h1>Reject Sale</h1>

<%= simple_form_for @transaction, :url => create_seller_sale_rejection_path, :method => :put do |f| %>
  <br class="spacer" />
  <p>
    Note: It is not recommended to reject a sale unless you have a good reason to do so.  Too many rejected sales can reflect poorly on a seller's profile.  If you are missing a few items from a sale, it is recommended that you <%= link_to "propose modifications", seller_sale_modification_path(@transaction) %> to the sale instead if rejecting it. 
  </p>
  <br class="spacer" />
  <br class="spacer" />  
  
  <div class="inputs">
    <%= f.input :rejection_reason,    :label => "Reason for Rejection",
                                      :as => :select,
                                      :collection => [["I no longer have these items", "availability"], ["I do not have time to ship items", "time"], ["Other", "other"]],
                                      :include_blank => false ,
                                      :input_html => {:class => 'chzn-select'},
                                      :required => true %>
    <br class="spacer"/>                        
    <%= f.input :response_message,   :label => "Optional Message",
                                      :as => :text,
                                      :hint => "Optional message to the buyer",
                                      :required => false,
                                      :input_html => {:max_length => '255'} %>
  </div>
  <br class="spacer"/>
  <br class="spacer"/>
  <br class="spacer"/>  

  <%= f.button :submit, "Reject Sale", :class => "center" %>

<% end %>
