<% if @error %>
  alert("Sorry, our shipping label system is not working at the moment... please try again later.");
<% else %>
  //$("#shipping_label_button").html('<%= link_to "View Shipping Label", create_shipping_label_path(@transaction), :target => "_blank", :class => "button ma-s", :style => "display:inline-block;" %>');
  var template = '<%= escape_javascript render :template => "mtg/transactions/show", :formats => [:html] %>'
  $('#center_bar').html(template);  
<% end %>