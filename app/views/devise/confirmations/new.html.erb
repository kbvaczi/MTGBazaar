<h1>Didn't receive confirmation instructions?</h1>
  

<br class="spacer"/>
<%= simple_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f| %>

  <% flash[:error] = "There were one or more errors while trying to process your request" if !devise_error_messages!.empty? %>
  <h2>Check your spam box</h2>
  <p>If you did not receive an email from us including account confirmation instructions, please check to verify your email service did not classify the message as spam.</p>
  <h2>Resend confirmation</h2>
  <p>If you would like us to resend confirmation instructions, please fill out your email address below and click "Resend". </p>
  <br class="spacer"/>

  <%= f.input :email ,                :required => 'true' %>
  <br class="spacer"/>
  <%= f.submit "Resend" , :class => 'center' %>

<% end %>

<%= render :partial => "devise/shared/links" %>
