<div class="left">	
  
  <div id="rightBar3" class="hidden">
  
  	<% cache("top_cards", :expires_in => 1.days) do %>  
  	  <div class="right_bar3">	
  	    <h1>Top Cards</h1>
  		  <% top_cards = Mtg::Card.includes(:set, :statistics).order('mtg_card_statistics.number_sales DESC').limit(7) %>
  		  <div class="mr-s">
  		    <table class="data fixed">
  		      <% top_cards.each do |card| %>
  		        <tr style="margin:0;padding:0;">
  		          <td width="35px" style="padding:0;vertical-align:top;"> <%= display_set_symbol(card.set) %> </td>
  		          <td              style="padding:1px;text-align:left;text-overflow:ellipsis;max-width:150px;white-space:nowrap;overflow:hidden;"> <%= link_to display_name(card.name), mtg_card_path(card) %> </td>
  		          <td width="50px" style="padding:1px;text-align:right;"> <%= number_to_currency(card.statistics.price_med) %> </td>          
  		        </tr>        
  		      <% end %>
  		    </table>
  		  </div>
  		</div>
  	<% end %>	
	
  	<% cache("recent_expansions", :expires_in => 1.days) do %>  	
  	  <div class="right_bar3">	
    	  <h1>Recent Expansions</h1>
    	  <% recent_added = Mtg::Set.order('release_date DESC').limit(7) %>
    	  <div class="mr-s">
          <table class="data fixed">
            <% recent_added.each do |set| %>
              <tr style="margin:0px;padding:0;">
  		          <td width="35px" style="padding:1px;"> <%= display_set_symbol(set) %> </td>
                <td style="margin:0;padding:1px;text-align:left;text-overflow: ellipsis;max-width:150px;white-space: nowrap;overflow:hidden;"> <%= link_to set.name, mtg_cards_path(:set => set.code) %> </td>          
              </tr>
           	<% end %>
          </table>
      	</div>
    	</div>
  	<% end %>
  	
  	<% cache("top_sellers", :expires_in => 1.days) do %>  	
  	  <div class="right_bar3">	
    	  <h1>Top Sellers</h1>
    	  <% top_sellers = User.active.includes(:statistics).order("user_statistics.number_sales DESC").limit(7) %>
    	  <div class="mt-s mr-s ml-s">
          <table class="data">
            <% top_sellers.each do |seller| %>
              <tr style="margin:2px;padding:0;">
                <td style="margin:1px;padding:1px;text-align:left;"> <%= link_to display_name(seller.username), user_path(seller) %> </td>
                <td style="margin:1px;padding:1px;text-align:right;"> <%= "(#{seller.statistics.number_sales} / #{seller.statistics.display_approval_percent})" %> </td>  
              </tr>
           	<% end %>
          </table>
      	</div>
    	</div>
  	<% end %>

  </div>  
  
</div>