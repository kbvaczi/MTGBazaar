<h1>Listing deposits</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @deposits.each do |deposit| %>
  <tr>
    <td><%= link_to 'Show', users_deposit %></td>
    <td><%= link_to 'Edit', edit_users_deposit_path(users_deposit) %></td>
    <td><%= link_to 'Destroy', users_deposit, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Balance transfer', new_users_deposit_path %>
